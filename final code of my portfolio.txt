<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Varsha Padaki · Enhanced Portfolio</title>
  <meta name="description" content="Portfolio of Varsha Padaki – Web Developer & CS Undergrad" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b0d10;
      --fg:#e8edf2;
      --muted:#9fb0c3;
      --card:#11151a;
      --accent:#6ae3ff;
      --accent-2:#b388ff;
      --accent-3:#ff6b9d;
      --ring:rgba(106,227,255,.35);
      --shadow:0 20px 40px rgba(0,0,0,.35);
      --radius:18px;
      --transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
    }
    
    :root.light{
      --bg:#f6f8fb;
      --fg:#0c1116;
      --muted:#5c6a79;
      --card:#ffffff;
      --accent:#0ea5e9;
      --accent-2:#7c3aed;
      --accent-3:#e11d48;
      --ring:rgba(14,165,233,.25);
      --shadow:0 12px 28px rgba(2,6,23,.12);
    }

    *{
      box-sizing:border-box;
    }
    
    html{
      scroll-behavior: smooth;
    }
    
    body{
      margin:0;
      font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial;
      background:
        radial-gradient(1200px 900px at 90% -10%, rgba(106,227,255,.10), transparent 60%),
        radial-gradient(900px 700px at 10% -10%, rgba(179,136,255,.10), transparent 60%),
        radial-gradient(800px 600px at 50% 100%, rgba(255,107,157,.08), transparent 70%),
        var(--bg);
      color:var(--fg);
      line-height:1.6;
      overflow-x: hidden;
    }
    
    a{color:inherit; transition: var(--transition);}
    .container{max-width:1100px;margin-inline:auto;padding:24px}

    /* Enhanced Navigation */
    .nav{
      position:sticky;
      top:0;
      backdrop-filter:saturate(150%) blur(20px);
      background:linear-gradient(to bottom, rgba(255,255,255,.06), rgba(255,255,255,0));
      border-bottom:1px solid rgba(255,255,255,.12);
      z-index:100;
      transition: var(--transition);
    }
    
    .nav.scrolled {
      background: rgba(11,13,16,.9);
      box-shadow: 0 8px 32px rgba(0,0,0,.3);
    }
    
    .nav-inner{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:16px;
      position: relative;
    }
    
    .brand{
      display:flex;
      align-items:center;
      gap:12px;
      font-weight:800;
      letter-spacing:.5px;
      transition: var(--transition);
    }
    
    .brand:hover {
      transform: scale(1.05);
    }
    
    .brand .logo{
      width:40px;
      height:40px;
      border-radius:14px;
      background:linear-gradient(135deg,var(--accent),var(--accent-2),var(--accent-3));
      box-shadow:0 8px 24px var(--ring);
      position: relative;
      overflow: hidden;
      transition: var(--transition);
    }
    
    .brand .logo::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(45deg, transparent, rgba(255,255,255,.3), transparent);
      transform: rotate(45deg);
      transition: var(--transition);
      opacity: 0;
    }
    
    .brand:hover .logo::before {
      animation: shine 0.8s ease-in-out;
    }
    
    @keyframes shine {
      0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); opacity: 0; }
      50% { opacity: 1; }
      100% { transform: translateX(100%) translateY(100%) rotate(45deg); opacity: 0; }
    }
    
    .nav-links{
      display:flex;
      gap:4px;
      flex-wrap:wrap;
    }
    
    .nav-links a{
      padding:12px 16px;
      border-radius:12px;
      text-decoration:none;
      color:var(--fg);
      border:1px solid transparent;
      position: relative;
      transition: var(--transition);
      overflow: hidden;
    }
    
    .nav-links a::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,.1), transparent);
      transition: left 0.5s ease;
    }
    
    .nav-links a:hover::before {
      left: 100%;
    }
    
    .nav-links a:hover{
      border-color:var(--ring);
      background:rgba(255,255,255,.08);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(106,227,255,.2);
    }
    
    .toggle{
      display:inline-flex;
      gap:10px;
      align-items:center;
      padding:10px 14px;
      border-radius:12px;
      border:1px solid rgba(255,255,255,.12);
      cursor:pointer;
      user-select:none;
      background: rgba(255,255,255,.03);
      transition: var(--transition);
    }
    
    .toggle:hover {
      background: rgba(255,255,255,.08);
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(255,255,255,.1);
    }

    /* Enhanced Hero Section */
    .hero{
      display:grid;
      grid-template-columns:1.2fr .8fr;
      gap:40px;
      align-items:center;
      padding:80px 0;
      position: relative;
    }
    
    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: radial-gradient(circle at 20% 80%, rgba(106,227,255,.15), transparent 50%),
                  radial-gradient(circle at 80% 20%, rgba(179,136,255,.15), transparent 50%);
      animation: heroGlow 8s ease-in-out infinite alternate;
      pointer-events: none;
    }
    
    @keyframes heroGlow {
      0% { opacity: 0.5; transform: scale(1); }
      100% { opacity: 0.8; transform: scale(1.1); }
    }
    
    .tag{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:8px 16px;
      border-radius:24px;
      border:1px solid var(--ring);
      background:rgba(255,255,255,.05);
      font-size:13px;
      color:var(--muted);
      backdrop-filter: blur(10px);
      animation: slideInUp 0.8s ease-out 0.2s both;
      position: relative;
      overflow: hidden;
    }
    
    .tag::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(106,227,255,.3), transparent);
      animation: tagShine 3s ease-in-out infinite;
    }
    
    @keyframes tagShine {
      0%, 100% { left: -100%; }
      50% { left: 100%; }
    }
    
    .title{
      font-size:clamp(32px,5vw,56px);
      line-height:1.1;
      margin:20px 0 12px;
      font-weight:900;
      background: linear-gradient(135deg, var(--fg), var(--accent), var(--accent-2));
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      animation: slideInUp 0.8s ease-out 0.4s both;
    }
    
    .subtitle{
      font-size:clamp(16px,2.2vw,22px);
      color:var(--muted);
      animation: slideInUp 0.8s ease-out 0.6s both;
      line-height: 1.5;
    }
    
    .cta{
      display:flex;
      gap:16px;
      margin-top:28px;
      flex-wrap:wrap;
      animation: slideInUp 0.8s ease-out 0.8s both;
    }
    
    .btn{
      padding:14px 20px;
      border-radius:16px;
      border:1px solid transparent;
      text-decoration:none;
      font-weight:600;
      position: relative;
      transition: var(--transition);
      overflow: hidden;
      cursor: pointer;
      background: none;
      color: inherit;
      font-family: inherit;
      font-size: inherit;
    }
    
    .btn::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      background: radial-gradient(circle, rgba(255,255,255,.3), transparent);
      transition: all 0.5s ease;
      transform: translate(-50%, -50%);
    }
    
    .btn:hover::before {
      width: 300px;
      height: 300px;
    }
    
    .btn.primary{
      background:linear-gradient(135deg,var(--accent),var(--accent-2));
      color:#0b0d10;
      box-shadow:0 12px 28px var(--ring);
    }
    
    .btn.primary:hover {
      transform: translateY(-3px);
      box-shadow:0 16px 36px var(--ring);
    }
    
    .btn.ghost{
      border-color:var(--ring);
      background: rgba(255,255,255,.05);
      backdrop-filter: blur(10px);
    }
    
    .btn.ghost:hover {
      background: rgba(255,255,255,.1);
      transform: translateY(-3px);
      box-shadow: 0 8px 24px rgba(106,227,255,.2);
    }

    /* Loading states for form */
    .btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }
    
    .btn.loading {
      position: relative;
    }
    
    .btn.loading::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 20px;
      height: 20px;
      margin: -10px 0 0 -10px;
      border: 2px solid transparent;
      border-top: 2px solid currentColor;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }
    
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    /* Enhanced Blob Animation */
    .blob-wrap{
      position:relative;
      height:400px;
      animation: slideInRight 0.8s ease-out 1s both;
    }
    
    .blob{
      position:absolute;
      inset:0;
      background:
        radial-gradient(140px 140px at 30% 30%,rgba(106,227,255,.9),transparent 70%),
        radial-gradient(160px 160px at 70% 60%,rgba(179,136,255,.9),transparent 70%),
        radial-gradient(120px 120px at 50% 80%,rgba(255,107,157,.8),transparent 70%),
        linear-gradient(135deg,rgba(255,255,255,.08),rgba(255,255,255,.04));
      border-radius:28px;
      filter:saturate(120%) blur(1px);
      box-shadow:var(--shadow);
      overflow:hidden;
      animation: blobPulse 4s ease-in-out infinite;
    }
    
    @keyframes blobPulse {
      0%, 100% { transform: scale(1) rotate(0deg); }
      50% { transform: scale(1.05) rotate(1deg); }
    }
    
    .orbs{
      position:absolute;
      inset:-30% -20%;
      background:
        radial-gradient(500px 500px at 20% 20%, rgba(106,227,255,.15), transparent 80%),
        radial-gradient(600px 600px at 80% 70%, rgba(179,136,255,.15), transparent 80%),
        radial-gradient(400px 400px at 60% 40%, rgba(255,107,157,.12), transparent 80%);
      animation: orbsRotate 20s linear infinite;
    }
    
    @keyframes orbsRotate {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    
    .float{
      position:absolute;
      width:120px;
      height:120px;
      background:linear-gradient(135deg,var(--accent),var(--accent-2),var(--accent-3));
      border-radius:30px;
      filter:blur(2px) saturate(120%);
      opacity:.7;
      animation:float 8s ease-in-out infinite;
    }
    
    .float:nth-child(1){
      top:15%;
      left:8%;
      animation-delay:0s;
      animation-duration: 10s;
    }
    
    .float:nth-child(2){
      top:55%;
      left:68%;
      animation-delay:2s;
      animation-duration: 12s;
    }
    
    .float:nth-child(3){
      top:25%;
      left:78%;
      animation-delay:4s;
      animation-duration: 14s;
    }
    
    @keyframes float{
      0%,100%{transform:translateY(-15px) rotate(0deg) scale(1);}
      25%{transform:translateY(10px) rotate(2deg) scale(1.05);}
      50%{transform:translateY(15px) rotate(-1deg) scale(0.95);}
      75%{transform:translateY(-5px) rotate(1deg) scale(1.02);}
    }

    /* Enhanced Sections */
    section{
      padding:72px 0;
      position: relative;
    }
    
    .section-title{
      font-size:32px;
      font-weight:800;
      margin-bottom:24px;
      position: relative;
      display: inline-block;
    }
    
    .section-title::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--accent), var(--accent-2));
      border-radius: 2px;
      transition: width 0.8s ease;
    }
    
    .section-title.animate::after {
      width: 100%;
    }
    
    .grid{
      display:grid;
      gap:20px;
    }
    
    .grid.cols-3{grid-template-columns:repeat(3,1fr);}
    .grid.cols-2{grid-template-columns:repeat(2,1fr);}
    
    @media(max-width:900px){
      .hero{grid-template-columns:1fr;}
      .grid.cols-3{grid-template-columns:repeat(2,1fr);}
      .blob-wrap{height:300px;}
    }
    
    @media(max-width:640px){
      .grid.cols-2,.grid.cols-3{grid-template-columns:1fr;}
      .hero{padding:60px 0;}
      .nav-links{display: none;}
    }

    /* Enhanced Cards */
    .card{
      background:linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
      border:1px solid rgba(255,255,255,.12);
      border-radius:var(--radius);
      padding:24px;
      box-shadow:var(--shadow);
      position:relative;
      overflow:hidden;
      transition: var(--transition);
    }
    
    .card::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,.1), transparent);
      transition: left 0.8s ease;
    }
    
    .card:hover::before {
      left: 100%;
    }
    
    .card::after{
      content:"";
      position:absolute;
      inset:-1px;
      border-radius:inherit;
      background:linear-gradient(135deg, transparent, rgba(255,255,255,.15), transparent);
      mask:linear-gradient(#fff,#fff) content-box,linear-gradient(#fff,#fff);
      -webkit-mask-composite:xor;
      mask-composite:exclude;
      padding:1px;
      pointer-events:none;
      transition: var(--transition);
    }
    
    .card:hover{
      transform:translateY(-8px) scale(1.02);
      box-shadow: 0 24px 48px rgba(0,0,0,.2);
      border-color: rgba(106,227,255,.3);
    }

    /* Enhanced Skills Section */
    .chip{
      display:inline-flex;
      gap:8px;
      align-items:center;
      padding:8px 14px;
      border-radius:20px;
      background:rgba(255,255,255,.08);
      border:1px solid rgba(255,255,255,.15);
      font-size:12px;
      color:var(--muted);
      transition: var(--transition);
      position: relative;
      overflow: hidden;
    }
    
    .chip::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      opacity: 0;
      transition: var(--transition);
    }
    
    .chip:hover::before {
      opacity: 0.1;
    }
    
    .chip:hover {
      transform: translateY(-2px);
      border-color: var(--accent);
      box-shadow: 0 4px 12px rgba(106,227,255,.3);
    }
    
    .stack{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
      margin-top:12px;
    }

    /* Enhanced Projects */
    .projects .card {
      transition: var(--transition);
      cursor: pointer;
    }
    
    .projects .card a{
      display:flex;
      gap:16px;
      align-items:flex-start;
      text-decoration:none;
      height: 100%;
    }
    
    .projects .card:hover {
      background: linear-gradient(180deg, rgba(255,255,255,.1), rgba(255,255,255,.05));
    }
    
    .projects .card .title{
      font-size:20px;
      margin:0 0 8px;
      background: linear-gradient(135deg, var(--fg), var(--accent));
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    /* Skill Bars Animation */
    .skills .bar{
      height:12px;
      background:rgba(255,255,255,.1);
      border-radius:20px;
      overflow:hidden;
      position: relative;
    }
    
    .skills .fill{
      height:100%;
      width:0;
      background:linear-gradient(90deg,var(--accent),var(--accent-2),var(--accent-3));
      border-radius:inherit;
      box-shadow:0 8px 22px var(--ring);
      position: relative;
      transition: width 1.5s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .skills .fill::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,.3), transparent);
      animation: fillShine 2s ease-in-out infinite;
    }
    
    @keyframes fillShine {
      0% { transform: translateX(-100%); }
      50% { transform: translateX(0%); }
      100% { transform: translateX(100%); }
    }

    /* Enhanced Links */
    .links{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
    }
    
    .links a{
      padding:10px 16px;
      border-radius:14px;
      border:1px solid rgba(255,255,255,.15);
      text-decoration:none;
      transition: var(--transition);
      background: rgba(255,255,255,.05);
      backdrop-filter: blur(10px);
    }
    
    .links a:hover {
      background: rgba(255,255,255,.1);
      border-color: var(--accent);
      transform: translateY(-3px);
      box-shadow: 0 6px 16px rgba(106,227,255,.2);
    }

    /* Enhanced Contact Form */
    form{
      display:grid;
      gap:16px;
    }
    
    input, textarea{
      width:100%;
      padding:14px 18px;
      border-radius:14px;
      border:1px solid rgba(255,255,255,.15);
      background:rgba(255,255,255,.05);
      color:var(--fg);
      transition: var(--transition);
      backdrop-filter: blur(10px);
      font-family: inherit;
    }
    
    input:focus, textarea:focus {
      outline: none;
      border-color: var(--accent);
      background: rgba(255,255,255,.08);
      box-shadow: 0 0 0 3px rgba(106,227,255,.1);
    }
    
    input::placeholder, textarea::placeholder{
      color:var(--muted);
    }
    
    textarea{
      min-height:120px;
      resize:vertical;
    }
    
    .submit{
      justify-self:start;
    }

    /* Success/Error Messages */
    .form-message {
      padding: 12px 16px;
      border-radius: 12px;
      margin-top: 16px;
      font-weight: 500;
      animation: slideInUp 0.3s ease-out;
    }
    
    .form-message.success {
      background: rgba(34, 197, 94, 0.1);
      border: 1px solid rgba(34, 197, 94, 0.3);
      color: #22c55e;
    }
    
    .form-message.error {
      background: rgba(239, 68, 68, 0.1);
      border: 1px solid rgba(239, 68, 68, 0.3);
      color: #ef4444;
    }

    /* Enhanced Footer */
    footer{
      padding:48px 0;
      color:var(--muted);
      border-top: 1px solid rgba(255,255,255,.1);
      margin-top: 40px;
    }

    /* Animation Classes */
    @keyframes slideInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    @keyframes slideInRight {
      from {
        opacity: 0;
        transform: translateX(50px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }
    
    @keyframes slideInLeft {
      from {
        opacity: 0;
        transform: translateX(-50px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }
    
    .reveal{
      opacity:0;
      transform:translateY(30px);
      transition:all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .reveal.in{
      opacity:1;
      transform:translateY(0);
    }
    
    .stagger-item {
      opacity: 0;
      transform: translateY(20px);
      transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .stagger-item.in {
      opacity: 1;
      transform: translateY(0);
    }

    /* Typewriter Effect */
    .type{
      border-right:3px solid var(--accent);
      white-space:nowrap;
      overflow:hidden;
    }
    
    @keyframes blink {
      0%, 50% { border-color: var(--accent); }
      51%, 100% { border-color: transparent; }
    }
    
    .type.complete {
      animation: blink 1s infinite;
    }

    /* Let's Connect Feature */
    .connect-section {
      background: linear-gradient(135deg, rgba(106,227,255,.05), rgba(179,136,255,.05));
      border-radius: 24px;
      padding: 40px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .connect-section::before {
      content: '';
      position: absolute;
      inset: -1px;
      background: linear-gradient(135deg, var(--accent), var(--accent-2), var(--accent-3));
      border-radius: inherit;
      mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
      -webkit-mask-composite: xor;
      mask-composite: exclude;
      padding: 1px;
      animation: borderGlow 3s ease-in-out infinite;
    }
    
    @keyframes borderGlow {
      0%, 100% { opacity: 0.5; }
      50% { opacity: 1; }
    }
    
    .connect-title {
      font-size: 28px;
      font-weight: 800;
      margin-bottom: 16px;
      background: linear-gradient(135deg, var(--accent), var(--accent-2), var(--accent-3));
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    
    .social-links {
      display: flex;
      justify-content: center;
      gap: 16px;
      flex-wrap: wrap;
      margin-top: 24px;
    }
    
    .social-link {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 12px 20px;
      border-radius: 16px;
      text-decoration: none;
      background: rgba(255,255,255,.05);
      border: 1px solid rgba(255,255,255,.15);
      transition: var(--transition);
      backdrop-filter: blur(10px);
    }
    
    .social-link:hover {
      background: rgba(255,255,255,.1);
      transform: translateY(-4px) scale(1.05);
      box-shadow: 0 8px 24px rgba(106,227,255,.3);
    }
    
    .social-link.email:hover { border-color: #ea4335; }
    .social-link.linkedin:hover { border-color: #0077b5; }
    .social-link.github:hover { border-color: #333; }
    .social-link.phone:hover { border-color: #25d366; }

    /* Scroll Progress Bar */
    .progress-bar {
      position: fixed;
      top: 0;
      left: 0;
      width: 0%;
      height: 3px;
      background: linear-gradient(90deg, var(--accent), var(--accent-2), var(--accent-3));
      z-index: 1000;
      transition: width 0.1s ease;
    }

    /* Back to Top Button */
    .back-to-top.show {
      opacity: 1;
      transform: scale(1);
    }
    
    .back-to-top:hover {
      transform: scale(1.1);
      box-shadow: 0 6px 16px rgba(106,227,255,.4);
    }

    /* Particle Background Effect */
    .particles {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: -1;
    }
    
    .particle {
      position: absolute;
      width: 2px;
      height: 2px;
      background: var(--accent);
      border-radius: 50%;
      animation: particle-float 15s linear infinite;
      opacity: 0.3;
    }
    
    @keyframes particle-float {
      0% {
        transform: translateY(100vh) translateX(0);
        opacity: 0;
      }
      10% {
        opacity: 0.3;
      }
      90% {
        opacity: 0.3;
      }
      100% {
        transform: translateY(-10px) translateX(100px);
        opacity: 0;
      }
    }
  </style>
</head>
<body>
  <!-- Progress Bar -->
  <div class="progress-bar" id="progressBar"></div>
  
  <!-- Particle Background -->
  <div class="particles" id="particles"></div>

  <header class="nav" id="nav">
    <div class="container nav-inner">
      <div class="brand">
        <div class="logo"></div> 
        <span>Varsha Padaki</span>
      </div>
      <nav class="nav-links">
        <a href="#about">About</a>
        <a href="#skills">Skills</a>
        <a href="#projects">Projects</a>
        <a href="#education">Education</a>
        <a href="#achievements">Achievements</a>
        <a href="#connect">Connect</a>
        <a href="#contact">Contact</a>
        <button id="themeToggle" class="toggle" aria-label="Toggle theme">🌙 <span class="hide-sm">Theme</span></button>
      </nav>
    </div>
  </header>

  <main class="container">
    <section class="hero">
      <div>
        <div class="tag">✨ CS Undergrad · Web Developer · Problem Solver</div>
        <h1 class="title type" id="typewriter">Hi, I'm Varsha Padaki</h1>
        <h2 class="subtitle">I build innovative, user‑friendly software.</h2>
        <p class="subtitle">Passionate software engineer specializing in web applications, data solutions</p>
        <div class="cta">
          <a class="btn primary" href="https://drive.google.com/file/d/1uZuynoW64pnaQYDyg2t084oqPSdSsAN4/view?usp=sharing" target="_blank">📄 Download Resume</a>
          <a class="btn ghost" href="#connect">🚀 Let's Connect</a>
        </div>
      </div>
      <div class="blob-wrap">
        <div class="blob">
          <div class="orbs"></div>
          <div class="float"></div>
          <div class="float"></div>
          <div class="float"></div>
        </div>
      </div>
    </section>

    <section id="about" class="reveal">
      <h2 class="section-title">🚀 About Me</h2>
      <div class="card">
        <p>
          I'm a <strong>Computer Science Engineering student</strong> and an aspiring developer passionate about creating 
          <strong>impactful digital solutions</strong>. As a fresher, I am building strong foundations in 
          <strong>full-stack web development</strong> while also exploring the world of 
          <strong>artificial intelligence and machine learning</strong>.
        </p>
        <p>
          I love <strong>learning, problem-solving, and developing solutions</strong> that blend 
          <strong>clean code with great user experiences</strong>. With a growth-oriented mindset, I continuously explore 
          new technologies and industry best practices to strengthen my skills and contribute effectively to 
          <strong>future opportunities</strong>.
        </p>
      </div>
    </section>

    <section id="skills" class="skills reveal">
      <h2 class="section-title">💻 Technical Skills</h2>
      <div class="grid cols-2">
        <div class="card stagger-item">
          <h3>🔧 Programming Languages</h3>
          <div class="stack">
            <span class="chip">C</span>
            <span class="chip">C++</span>
            <span class="chip">Java</span>
            <span class="chip">Python</span>
            <span class="chip">JavaScript</span>
          </div>
        </div>
        <div class="card stagger-item">
          <h3>🚀 Frameworks & Libraries</h3>
          <div class="stack">
            <span class="chip">React.js</span>
            <span class="chip">Node.js</span>
            <span class="chip">Flask</span>
            <span class="chip">NumPy</span>
            <span class="chip">Pandas</span>
            <span class="chip">Matplotlib</span>
            <span class="chip">HTML5</span>
            <span class="chip">CSS3</span>
          </div>
        </div>
        <div class="card stagger-item">
          <h3>🛠️ Tools & Platforms</h3>
          <div class="stack">
            <span class="chip">Git</span>
            <span class="chip">GitHub</span>
            <span class="chip">VS Code</span>
            <span class="chip">Jupyter</span>
            <span class="chip">Eclipse</span>
            <span class="chip">AWS</span>
            <span class="chip">Docker</span>
          </div>
        </div>
        <div class="card stagger-item">
          <h3>🗄️ Databases</h3>
          <div class="stack">
            <span class="chip">MySQL</span>
            <span class="chip">MongoDB</span>
            <span class="chip">PostgreSQL</span>
            <span class="chip">SQLite</span>
          </div>
        </div>
      </div>
    </section>

    <section id="projects" class="projects reveal">
      <h2 class="section-title">🔥 Featured Projects</h2>
      <div class="grid cols-2">
        <div class="card tilt stagger-item">
          <a href="https://github.com/varshapadaki/API-Endpoint-Tester.git" target="_blank" rel="noreferrer">
            <div>
              <h3 class="title">🔧 API Endpoint Tester</h3>
              <p>Comprehensive Python testing suite for CRUD operations with automated response validation, performance metrics, and detailed reporting capabilities.</p>
              <div class="stack">
                <span class="chip">Python</span>
                <span class="chip">Requests</span>
                <span class="chip">JSON</span>
                <span class="chip">Testing</span>
              </div>
            </div>
          </a>
        </div>
        <div class="card tilt stagger-item">
          <a href="https://github.com/varshapadaki/utility-management-system.git" target="_blank" rel="noreferrer">
            <div>
              <h3 class="title">🏢 Utility Management System</h3>
              <p>Full-stack web application with Flask backend, MySQL database, location-based filtering, image upload functionality, and role-based dashboards.</p>
              <div class="stack">
                <span class="chip">Python</span>
                <span class="chip">Flask</span>
                <span class="chip">MySQL</span>
                <span class="chip">Bootstrap</span>
              </div>
            </div>
          </a>
        </div>
        <div class="card tilt stagger-item">
          <a href="https://github.com/varshapadaki/Student_Grading_System.git" target="_blank" rel="noreferrer">
            <div>
              <h3 class="title">🎓 Student Grading System</h3>
              <p>Interactive admin portal for student management with dynamic grade calculation, progress tracking, and responsive design using Flask templating.</p>
              <div class="stack">
                <span class="chip">Python</span>
                <span class="chip">Flask</span>
                <span class="chip">CSS3</span>
              </div>
            </div>
          </a>
        </div>
        <div class="card tilt stagger-item">
          <a href="https://github.com/varshapadaki/Linkedin_title_cleaner.git" target="_blank" rel="noreferrer">
            <div>
              <h3 class="title">🤖 LinkedIn Title Cleaner</h3>
              <p>Machine learning solution for cleaning and categorizing job titles with natural language processing, data preprocessing, and model training capabilities.</p>
              <div class="stack">
                <span class="chip">Python</span>
                <span class="chip">Pandas</span>
                <span class="chip">Scikit-learn</span>
                <span class="chip">NLP</span>
              </div>
            </div>
          </a>
        </div>
      </div>
    </section>

    <section id="education" class="reveal">
      <h2 class="section-title">🎓 Education</h2>
      <div class="grid cols-2">
        <div class="card stagger-item">
          <h3>🏛️ Ballari Institute of Technology & Management</h3>
          <p><strong>Bachelor of Engineering</strong> - Computer Science<br>
          📅 Nov 2022 – Jun 2026<br>
          🌟 <strong>CGPA: 8.6/10</strong></p>
        </div>
        <div class="card stagger-item">
          <h3>🏫 Shri Sharada Pre‑University College</h3>
          <p><strong>Pre-University Course</strong> - Science (PCMB)<br>
          📅 Jun 2020 – Apr 2022<br>
          🌟 <strong>Percentage: 79%</strong></p>
        </div>
        <div class="card stagger-item">
          <h3>🏫 St. Francis De‑Sales School</h3>
          <p><strong>Secondary School</strong> - SSLC<br>
          📅 Jun 2019 – Mar 2020<br>
          🌟 <strong>Percentage: 88%</strong></p>
        </div>
      </div>
    </section>

    <section id="achievements" class="reveal">
      <h2 class="section-title">🏆 Achievements & Certifications</h2>
      <div class="grid cols-2">
        <div class="card stagger-item">
          <h4>🤖 Google Cloud Career Launchpad</h4>
          <p><strong>Generative AI Track</strong><br>Advanced training in AI/ML technologies and cloud computing</p>
          <a class="btn ghost" href="https://drive.google.com/file/d/1AuH2o7LGULtL5jkKezv6sC8vbMLLZXMc/view?usp=sharing" target="_blank">View Certificate</a>
        </div>
        <div class="card stagger-item">
          <h4>🎯 National Level STTP 2024</h4>
          <p><strong>BITM × Brainovision × AICTE</strong><br>Short Term Training Program in emerging technologies</p>
          <a class="btn ghost" href="https://drive.google.com/file/d/15Qf1L4S5OB2_sBkCFJ3lA6YXQJn_I9Ar/view?usp=sharing" target="_blank">View Certificate</a>
        </div>
        <div class="card stagger-item">
          <h4>💼 IBM Enterprise AI Certification</h4>
          <p><strong>Getting Started with Enterprise‑grade AI</strong><br>Professional training in enterprise AI solutions</p>
          <a class="btn ghost" href="https://drive.google.com/file/d/1CtH0AOgPpHJ70DRsJ4m0NExJrmpNko-d/view?usp=sharing" target="_blank">View Certificate</a>
        </div>
        <div class="card stagger-item">
          <h4>☁️ AWS Cloud Quest</h4>
          <p><strong>Cloud Practitioner</strong><br>Hands-on cloud computing skills and AWS services</p>
          <a class="btn ghost" href="https://drive.google.com/file/d/1nBub6p76qpUX28oaNuZkRJ7ElW20dU8W/view?usp=sharing" target="_blank">View Certificate</a>
        </div>
        <div class="card stagger-item">
          <h4>📊 Deloitte Job Simulation</h4>
          <p><strong>Data Analytics</strong><br>Real-world data analysis and business intelligence projects</p>
          <a class="btn ghost" href="https://drive.google.com/file/d/1MSDRgH9wusTtevgCDYMXcUtIV4rC9CoT/view?usp=sharing" target="_blank">View Certificate</a>
        </div>
      </div>
    </section>

    <section id="connect" class="reveal">
      <h2 class="section-title">🌐 Let's Connect</h2>
      <div class="connect-section">
        <h3 class="connect-title">Ready to Collaborate?</h3>
        <p class="subtitle">I'm always excited to discuss new opportunities, share ideas, or collaborate on interesting projects. Feel free to reach out through any of these platforms!</p>
        
        <div class="social-links">
          <a href="mailto:varshapadaki04@gmail.com" class="social-link email">
            <span>📧</span>
            <span>Email Me</span>
          </a>
          <a href="http://www.linkedin.com/in/varsha-padaki-277b05319" target="_blank" rel="noreferrer" class="social-link linkedin">
            <span>💼</span>
            <span>LinkedIn</span>
          </a>
          <a href="https://github.com/varshapadaki" target="_blank" rel="noreferrer" class="social-link github">
            <span>🔗</span>
            <span>GitHub</span>
          </a>
          
        </div>
      </div>
    </section>

    <section id="contact" class="reveal">
      <h2 class="section-title">📬 Get In Touch</h2>
      <div class="grid cols-2">
        <div class="card stagger-item">
          <h3>📍 Contact Information</h3>
          <p><strong>📧 Email:</strong><br><a href="mailto:varshapadaki04@gmail.com">varshapadaki04@gmail.com</a></p>
          <p><strong>📞 Phone:</strong><br><a href="tel:+918088241986">+91 8088241986</a></p>
          <p><strong>🏠 Location:</strong><br>Behind Bhagvati Hospital, Padaki Layout, Koppal, Karnataka</p>
          <div class="resume" style="margin-top:20px">
            <a class="btn primary" href="https://drive.google.com/file/d/1uZuynoW64pnaQYDyg2t084oqPSdSsAN4/view?usp=sharing" target="_blank">📄 Download Resume</a>
          </div>
        </div>
        <div class="card stagger-item">
          <h3>💌 Send a Message</h3>
          <form id="contactForm">
            <input type="hidden" name="access_key" value="38b61162-8464-49a3-b526-e7a40c607b32">
            <input type="hidden" name="subject" value="New Portfolio Contact Message">
            <input type="hidden" name="from_name" value="Portfolio Contact Form">
            
            <input name="name" placeholder="Your name *" required />
            <input type="email" name="email" placeholder="Your email *" required />
            <textarea name="message" placeholder="Your message *" required></textarea>
            
            <!-- Honeypot field for spam protection -->
            <input type="checkbox" name="botcheck" style="display: none;">
            
            <button class="btn primary submit" type="submit" id="submitBtn">
              <span id="submitText">🚀 Send Message</span>
            </button>
          </form>
          <div id="formMessage"></div>
        </div>
      </div>
    </section>
  </main>

  <footer class="container">
    <div style="display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap">
      <span>© <span id="year"></span> Varsha Padaki • Made with 💖 and ☕</span>
      <div class="links">
        <a href="https://github.com/varshapadaki" target="_blank" rel="noreferrer">🔗 GitHub</a>
        <a href="https://leetcode.com/u/varshapadaki04/" target="_blank" rel="noreferrer">⚡ LeetCode</a>
        <a href="https://www.hackerrank.com/profile/varshapadaki04" target="_blank" rel="noreferrer">🏆 HackerRank</a>
      </div>
    </div>
  </footer>

  <!-- Back to Top Button -->
  <button class="back-to-top" id="backToTop" onclick="scrollToTop()">↑</button>

  <script>
    // Enhanced Theme Toggle
    const root = document.documentElement;
    const toggleBtn = document.getElementById('themeToggle');
    const stored = sessionStorage.getItem('theme') || 'dark';
    
    if(stored === 'light'){ 
      root.classList.add('light'); 
      toggleBtn.innerHTML = '🌞 <span class="hide-sm">Theme</span>'; 
    }
    
    toggleBtn.addEventListener('click', () => {
      root.classList.toggle('light');
      const isLight = root.classList.contains('light');
      sessionStorage.setItem('theme', isLight ? 'light' : 'dark');
      toggleBtn.innerHTML = isLight ? '🌞 <span class="hide-sm">Theme</span>' : '🌙 <span class="hide-sm">Theme</span>';
    });

    // Enhanced Typewriter Effect
    const typewriterEl = document.getElementById('typewriter');
    const fullText = typewriterEl.textContent.trim();
    typewriterEl.textContent = '';
    
    let i = 0;
    function typeWriter() {
      if (i < fullText.length) {
        typewriterEl.textContent += fullText.charAt(i);
        i++;
        setTimeout(typeWriter, 50);
      } else {
        typewriterEl.classList.add('complete');
      }
    }
    
    setTimeout(typeWriter, 800);

    // Scroll Progress Bar
    const progressBar = document.getElementById('progressBar');
    window.addEventListener('scroll', () => {
      const scrollTop = window.pageYOffset;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const scrollPercent = (scrollTop / docHeight) * 100;
      progressBar.style.width = scrollPercent + '%';
    });

    // Navigation Background on Scroll
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 100) {
        nav.classList.add('scrolled');
      } else {
        nav.classList.remove('scrolled');
      }
    });

    // Enhanced Scroll Reveal with Stagger Animation
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('in');
          
          // Animate section title underline
          const title = entry.target.querySelector('.section-title');
          if (title) {
            title.classList.add('animate');
          }
          
          // Stagger animation for items
          const staggerItems = entry.target.querySelectorAll('.stagger-item');
          staggerItems.forEach((item, index) => {
            setTimeout(() => {
              item.classList.add('in');
            }, index * 150);
          });
          
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

    // Skill Bars Animation
    const skillBars = document.querySelectorAll('.skills .fill');
    const skillObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const width = entry.target.dataset.width + '%';
          setTimeout(() => {
            entry.target.style.width = width;
          }, 500);
          skillObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });

    skillBars.forEach(bar => skillObserver.observe(bar));

    // Enhanced Card Tilt Effect
    const tiltCards = document.querySelectorAll('.tilt');
    tiltCards.forEach(card => {
      card.addEventListener('mousemove', (e) => {
        const rect = card.getBoundingClientRect();
        const x = (e.clientX - rect.left) / rect.width;
        const y = (e.clientY - rect.top) / rect.height;
        const rotateX = (y - 0.5) * 10;
        const rotateY = (x - 0.5) * -10;
        card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateZ(10px)`;
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) translateZ(0)';
      });
    });

    // Back to Top Button
    const backToTop = document.getElementById('backToTop');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 300) {
        backToTop.classList.add('show');
      } else {
        backToTop.classList.remove('show');
      }
    });

    function scrollToTop() {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    }

    // Enhanced Contact Form with Web3Forms Integration
    const form = document.getElementById('contactForm');
    const formMessage = document.getElementById('formMessage');
    const submitBtn = document.getElementById('submitBtn');
    const submitText = document.getElementById('submitText');
    
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      // Show loading state
      submitBtn.disabled = true;
      submitBtn.classList.add('loading');
      submitText.textContent = 'Sending...';
      
      // Clear previous messages
      formMessage.innerHTML = '';
      
      try {
        const formData = new FormData(form);
        
        // Send to Web3Forms
        const response = await fetch('https://api.web3forms.com/submit', {
          method: 'POST',
          body: formData
        });
        
        const data = await response.json();
        
        if (data.success) {
          // Success message
          formMessage.innerHTML = `
            <div class="form-message success">
              ✅ Message sent successfully! I'll get back to you soon.
            </div>
          `;
          form.reset();
        } else {
          throw new Error(data.message || 'Something went wrong');
        }
      } catch (error) {
        // Error message
        formMessage.innerHTML = `
          <div class="form-message error">
            ❌ Failed to send message. Please try again or email me directly at varshapadaki04@gmail.com
          </div>
        `;
        console.error('Form submission error:', error);
      } finally {
        // Reset button state
        submitBtn.disabled = false;
        submitBtn.classList.remove('loading');
        submitText.textContent = '🚀 Send Message';
        
        // Auto-hide message after 8 seconds
        setTimeout(() => {
          const messages = formMessage.querySelectorAll('.form-message');
          messages.forEach(msg => {
            msg.style.opacity = '0';
            msg.style.transform = 'translateY(-10px)';
            setTimeout(() => msg.remove(), 300);
          });
        }, 8000);
      }
    });

    // Particle Background Effect
    function createParticles() {
      const particleContainer = document.getElementById('particles');
      const particleCount = 50;
      
      for (let i = 0; i < particleCount; i++) {
        const particle = document.createElement('div');
        particle.className = 'particle';
        particle.style.left = Math.random() * 100 + '%';
        particle.style.animationDelay = Math.random() * 15 + 's';
        particle.style.animationDuration = (15 + Math.random() * 10) + 's';
        particleContainer.appendChild(particle);
      }
    }
    
    createParticles();

    // Smooth scrolling for navigation links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Set current year in footer
    document.getElementById('year').textContent = new Date().getFullYear();

    // Add loading animation completion
    window.addEventListener('load', () => {
      document.body.style.opacity = '1';
    });

    // Intersection Observer for performance
    if ('IntersectionObserver' in window) {
      // All observers are already implemented above
    } else {
      // Fallback for older browsers
      document.querySelectorAll('.reveal').forEach(el => el.classList.add('in'));
    }
  </script>
</body>
</html>top {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      border: none;
      color: #0b0d10;
      font-size: 20px;
      cursor: pointer;
      opacity: 0;
      transform: scale(0);
      transition: var(--transition);
      box-shadow: 0 4px 12px rgba(106,227,255,.3);
      z-index: 1000;
    }
    
    .back-to-